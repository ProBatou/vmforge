# Local configuration — never commit credentials
.env
.env.*
!.env.example

# Per-deployment state (contains IPs, MACs, UUIDs of your infra)
.deploy-state/

# SSH keys
*.pub
*.pem
*.p12
*.key

# macOS
.DS_Store
